#!/usr/bin/env bash
set -euo pipefail

BUMP_TYPE=${1:-patch}
REF=$(git branch --show-current)
if [[ -z "$REF" ]]; then
  REF=$(git rev-parse --abbrev-ref HEAD)
fi

gh workflow run release.yml --ref "$REF" -f bump="$BUMP_TYPE"
